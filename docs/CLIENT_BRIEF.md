## 10. Deferred to Phase 2 order (not in this delivery)

These features were discussed as possible additions but are **explicitly not part
of the current CAD $500 order**. They will require a separate second order:

- ❌ **Assist Me Mode** (global hotkey to trigger RAG assistance on-demand,
  even outside meetings)
- ❌ **Global system-wide hotkey** for quick access to the copilot
- ❌ **Background document monitoring** (auto-reindex when files change in
  watched folder without manual CLI command)
- ❌ **Internet search** (fallback to web search when no relevant document found — deferred to Phase 2 order)